# NetScope — Python dependencies
# Install with:  pip install -r requirements.txt
# Run with:      sudo python app.py

# Packet capture & protocol dissection
scapy==2.5.0

# Web framework
flask==3.0.3

# Real-time WebSocket support
flask-socketio==5.3.6

# Async worker required by Flask-SocketIO (Python 3.13 compatible)
gevent==25.9.1
gevent-websocket==0.10.1

# Used by gevent for DNS/SSL
dnspython==2.6.1
